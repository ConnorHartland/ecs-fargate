# AWS Config Module - Terraform and Provider Version Constraints
# Requirements: 11.5

terraform {
  required_version = ">= 1.0.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = ">= 5.0.0"
    }
  }
}
